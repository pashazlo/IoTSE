#pragma once

#include <stdint.h>
#include "esp_err.h"

#ifdef __cplusplus
extern "C" {
#endif

// ============================================================================
// Размеры дисплея в АЛЬБОМНОЙ ориентации (320x240)
// ============================================================================

#define DISPLAY_WIDTH   320   // Ширина в альбомном режиме
#define DISPLAY_HEIGHT  240   // Высота в альбомном режиме

// ============================================================================
// Публичные функции
// ============================================================================

/**
 * @brief Инициализация дисплея ST7789V
 *        Автоматически настраивает альбомный режим (320x240)
 */
esp_err_t display_init(void);

/**
 * @brief Отрисовка прямоугольной области
 * 
 * @param x1, y1   Верхний левый угол (включительно)
 * @param x2, y2   Нижний правый угол (исключительно)
 * @param color_data  Буфер RGB565 пикселей
 */
esp_err_t display_flush(int x1, int y1, int x2, int y2, const uint16_t *color_data);

/**
 * @brief Заливка всего экрана одним цветом
 */
esp_err_t display_fill_color(uint16_t color);

/**
 * @brief Установка ориентации дисплея
 * 
 * @param rotation: 0 = 0°, 1 = 90° (альбом), 2 = 180°, 3 = 270°
 * @return ESP_OK при успехе
 */
esp_err_t display_set_rotation(uint8_t rotation);

#ifdef __cplusplus
}
#endif
